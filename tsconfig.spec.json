{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["jest", "node"], // Specify types for Jest and Node.js
    "noEmit": true // Tests are not compiled to JS
  },
  "include": ["src/**/*.spec.ts", "test/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
